(function ($) {
  Drupal.behaviors.starterkit = {
    attach: function (context) {
    
    // -- Start jQuery closure.
    
    /**
     * Test this works.
     */
     alert('Hello world!');
     
     // -- End jQuery closure.
     
    }
  };
})(jQuery);
